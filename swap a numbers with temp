#include <stdio.h>
int main() {
    int a,b,temp;
    printf("enter the two values: \n");
    scanf("%d %d",&a,&b);
    temp=a;
    a=b;
    b=temp;
    printf("swap a number:a=%d,b=%d",a,b);

    return 0;
}
