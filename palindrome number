#include <stdio.h>
int main() {
 int a,b,c,d=0;
 printf("enter the value of a: ",&a);
 scanf("%d",&a);
 b=a;
 while (a!=0)
  {
      c=a%10;
      d=d*10+c;
      a=a/10;
  }
  if(d==b)
  {
      printf("the value is palindrome");
  }
  else{
      printf("the value is not  palindrome");
  }
    return 0;
}
