#include <stdio.h>

int main() {
    int a,b;
    printf("enter the value of a:\n",a);
    scanf("%d",&a);
    printf("enter the value of b:\n",b);
    scanf("%d",&b);
    if (a>b){
        printf("a is largest");
    } else if(b>a)
    {
        printf("b is largest");
    } else
{
    printf("a&b both are equal");
}
    return 0;
}
