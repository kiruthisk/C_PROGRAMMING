#include <stdio.h>

int main() {
   int arr[5]={21,34,67,98,90};
   int i;
   int largest=arr[0];
   int secondlargest=arr[0];
   for(i=1;i<5;i++)
   if(arr[i]>largest)
   {
       largest=arr[i];
   }
   for(i=0;i<5;i++)
   if(arr[i]>secondlargest && arr[i]<largest)
   {
       secondlargest=arr[i];
   }
   
    printf("%d second largest:\n",secondlargest);

    return 0;
}
